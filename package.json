{
  "name": "sgd-gadpr-lm",
  "private": true,
  "description": "Monorepo SGD-GADPR-LM — scripts de conveniencia; dependencias en backend/ y frontend/",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "free:3000": "npx --yes kill-port 3000",
    "dev:all": "npx --yes concurrently -n backend,frontend -c blue,magenta \"npm run start:dev\" \"npm run dev\"",
    "start:dev": "npm run start:dev --prefix backend",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "test": "npm run test --prefix backend",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:generate:clean": "npm run prisma:generate:clean --prefix backend"
  }
}
